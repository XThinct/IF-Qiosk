# Konfigurasi Port Server
PORT=8000

# Konfigurasi Database PostgreSQL
DB_HOST=localhost
DB_USER=postgres
DB_PASS=Latte201223
DB_NAME=ekiosk
DB_PORT=5432

JWT_SECRET=latte_mocha

FRONTEND_URL=http://localhost:5173

# Public URLs for cross-network QR code access (e.g. ngrok tunnels).
# Leave unset to use local IP (same-WiFi mode).
PUBLIC_BACKEND_URL=https://unjustly-resemble-paddling.ngrok-free.dev
PUBLIC_FRONTEND_URL=https://unjustly-resemble-paddling.ngrok-free.dev

# Environment
NODE_ENV=development
DB_LOGGING=false